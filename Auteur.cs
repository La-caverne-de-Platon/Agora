namespace Agora {
    public class Auteur {
        public string nom;
        public string url;

        public Auteur(string arg_nom, string arg_url)
        {
            nom = arg_nom;
            url = arg_url;
        }
    }
}
